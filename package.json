{
	"name": "sun-sync-cam",
	"version": "1.1.0",
	"main": "index.js",
	"scripts": {
		"start": "npx ts-node src/index.ts",
		"build": "tsc",
		"test": "echo \"Error: no test specified\" && exit 1",
		"format": "biome format --write",
		"format:check": "biome format",
		"lint": "biome lint && tsc --noEmit",
		"lint:fix": "biome lint --write"
	},
	"keywords": [],
	"author": "Bartosz Glubiak",
	"license": "ISC",
	"description": "Script to automatically synchronize dawn and dusk on ip camera",
	"devDependencies": {
		"@biomejs/biome": "1.9.4",
		"typescript": "^5.8.2"
	},
	"dependencies": {
		"axios": "^1.8.3",
		"moment-timezone": "^0.5.47",
		"prettier": "^3.5.3"
	}
}
